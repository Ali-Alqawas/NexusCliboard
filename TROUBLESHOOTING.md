# دليل استكشاف الأخطاء - NexusClip
# Troubleshooting Guide - NexusClip

---

## 🔴 المشاكل الشائعة / Common Issues

---

### المقبض الجانبي لا يظهر / Side Handle Not Showing

**الأعراض:**
- لا يوجد مقبض على حافة الشاشة
- التطبيق يعمل لكن بدون مقبض

**الحلول:**

1. **تحقق من صلاحية العرض فوق التطبيقات**
   ```
   الإعدادات → التطبيقات → NexusClip → صلاحيات خاصة → العرض فوق التطبيقات → تفعيل
   ```

2. **تحقق من تشغيل الخدمة**
   - افتح التطبيق مرة واحدة على الأقل
   - تأكد من ظهور إشعار "NexusClip Active"

3. **أعد تشغيل الخدمة**
   ```
   الإعدادات → التطبيقات → NexusClip → إيقاف إجباري
   ثم افتح التطبيق مجدداً
   ```

4. **أعد تشغيل الجهاز**
   - بعض الأجهزة تحتاج إعادة تشغيل بعد منح الصلاحيات

5. **تحقق من وضع توفير البطارية**
   - أضف NexusClip لقائمة الاستثناءات من تحسين البطارية

---

### الحافظة لا تُراقب / Clipboard Not Being Monitored

**الأعراض:**
- النصوص المنسوخة لا تظهر في Live Stream
- تبويب Live فارغ

**الحلول:**

1. **فعّل خدمة الوصول**
   ```
   الإعدادات → إمكانية الوصول → NexusClip → تفعيل
   ```

2. **أعد تشغيل الجهاز بعد تفعيل خدمة الوصول**
   - هذا ضروري على بعض الأجهزة

3. **تحقق من عدم وجود تطبيقات تحظر خدمات الوصول**
   - بعض تطبيقات الأمان تمنع خدمات الوصول

4. **جرب النسخ من تطبيق آخر**
   - بعض التطبيقات لا ترسل أحداث النسخ للنظام

---

### Virtual D-Pad لا يعمل / Virtual D-Pad Not Working

**الأعراض:**
- أزرار الأسهم لا تحرك المؤشر
- لا استجابة عند الضغط

**الحلول:**

1. **تأكد من تفعيل خدمة الوصول**
   - D-Pad يعتمد على AccessibilityService

2. **تأكد من وجود حقل نص مُركّز**
   - D-Pad يعمل فقط عندما يكون هناك حقل إدخال نشط

3. **جرب في تطبيق مختلف**
   - بعض التطبيقات تمنع التحكم الخارجي

---

### المزامنة مع Linux لا تعمل / Linux Sync Not Working

**الأعراض:**
- الأجهزة لا تكتشف بعضها
- النصوص لا تُنقل بين الأجهزة

**الحلول:**

1. **تأكد من أن الجهازين على نفس الشبكة**
   ```bash
   # على Linux
   ip addr show
   
   # على Android
   الإعدادات → WiFi → معلومات الشبكة
   ```

2. **تحقق من فتح Port 4040**
   ```bash
   # على Linux
   sudo ufw allow 4040/udp
   
   # أو
   sudo firewall-cmd --add-port=4040/udp --permanent
   ```

3. **شغّل الـ Daemon بوضع Verbose**
   ```bash
   python3 nexusclip_daemon.py -v
   ```

4. **تحقق من عدم وجود برنامج آخر يستخدم Port 4040**
   ```bash
   sudo netstat -tulpn | grep 4040
   # أو
   sudo ss -tulpn | grep 4040
   ```

5. **تأكد من تثبيت pyperclip بشكل صحيح**
   ```bash
   # على Linux
   sudo apt install xclip  # للأنظمة المبنية على Debian
   pip3 install pyperclip
   ```

---

### التطبيق يستهلك بطارية كثيرة / High Battery Consumption

**الأعراض:**
- استهلاك بطارية أكثر من 2% يومياً
- ظهور التطبيق في قائمة مستهلكي البطارية

**الحلول:**

1. **تحقق من عدم تشغيل الخزنة باستمرار**
   - أغلق الخزنة عند عدم الحاجة

2. **عطّل المزامنة إذا لم تكن مستخدمة**
   - من إعدادات التطبيق

3. **تحقق من إصدار التطبيق**
   - الإصدارات الأحدث تحتوي على تحسينات للأداء

---

### التطبيق يتوقف فجأة / App Crashes

**الأعراض:**
- التطبيق يُغلق بشكل مفاجئ
- رسالة "توقف NexusClip"

**الحلول:**

1. **امسح ذاكرة التخزين المؤقت**
   ```
   الإعدادات → التطبيقات → NexusClip → التخزين → مسح الذاكرة المؤقتة
   ```

2. **أعد تثبيت التطبيق**
   ```bash
   adb uninstall com.nexusclip.clip
   adb install nexusclip-release.apk
   ```

3. **تحقق من مساحة التخزين**
   - تأكد من وجود مساحة كافية

4. **تحقق من إصدار Android**
   - يتطلب Android 8.0 أو أحدث

---

### الخزنة الآمنة لا تفتح / Secure Vault Won't Open

**الأعراض:**
- طلب المصادقة البيومترية يفشل
- لا يمكن الوصول للبيانات المشفرة

**الحلول:**

1. **تحقق من إعدادات البيومترية**
   ```
   الإعدادات → الأمان → بصمة الإصبع / Face Unlock
   ```

2. **أعد تسجيل البصمة**
   - احذف البصمات القديمة وأضف جديدة

3. **استخدم PIN كبديل**
   - إذا فشلت البيومترية، استخدم PIN

---

## 🔧 أدوات التشخيص / Diagnostic Tools

### عرض سجلات التطبيق / View App Logs

```bash
# عرض سجلات NexusClip
adb logcat | grep -i nexusclip

# عرض سجلات الأخطاء فقط
adb logcat *:E | grep -i nexusclip

# حفظ السجلات لملف
adb logcat -d > nexusclip_logs.txt
```

### التحقق من حالة الخدمة / Check Service Status

```bash
# عرض الخدمات الجارية
adb shell dumpsys activity services | grep nexusclip
```

### التحقق من الصلاحيات / Check Permissions

```bash
# عرض صلاحيات التطبيق
adb shell dumpsys package com.nexusclip.clip | grep permission
```

---

## 📊 معلومات للدعم الفني / Technical Support Info

عند الإبلاغ عن مشكلة، يرجى توفير:

1. **معلومات الجهاز:**
   - الشركة المصنعة والموديل
   - إصدار Android
   - إصدار NexusClip

2. **خطوات إعادة إنتاج المشكلة:**
   - ماذا فعلت؟
   - ماذا توقعت؟
   - ماذا حدث فعلاً؟

3. **سجلات التطبيق:**
   ```bash
   adb logcat -d > logs.txt
   ```

4. **لقطات شاشة:**
   - للمشاكل البصرية

---

## 📞 التواصل / Contact

- **GitHub Issues:** [github.com/nexusclip/issues](https://github.com/nexusclip/issues)
- **Email:** support@nexusclip.app

---

## 🔄 إعادة ضبط التطبيق / Reset App

إذا لم تنجح أي من الحلول:

```bash
# إلغاء تثبيت كامل
adb uninstall com.nexusclip.clip

# مسح البيانات المتبقية
adb shell pm clear com.nexusclip.clip

# إعادة التثبيت
adb install nexusclip-release.apk
```

⚠️ **تحذير:** سيؤدي هذا لفقدان جميع البيانات المحفوظة!
