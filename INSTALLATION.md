# دليل التثبيت - NexusClip
# Installation Guide - NexusClip

---

## 📱 تثبيت التطبيق / App Installation

### الطريقة 1: من ملف APK

1. **تحميل APK**
   - قم بتحميل `nexusclip-release.apk` من صفحة الإصدارات

2. **تفعيل التثبيت من مصادر غير معروفة**
   - الإعدادات → الأمان → مصادر غير معروفة → تفعيل
   - أو: الإعدادات → التطبيقات → إذن تثبيت التطبيقات

3. **تثبيت التطبيق**
   - افتح ملف APK من مدير الملفات
   - اضغط "تثبيت"

### الطريقة 2: عبر ADB

```bash
# تأكد من تفعيل USB Debugging
adb devices

# تثبيت APK
adb install nexusclip-release.apk

# أو مع استبدال النسخة القديمة
adb install -r nexusclip-release.apk
```

---

## 🔐 تفعيل الصلاحيات / Enable Permissions

### صلاحية العرض فوق التطبيقات (إلزامية)

هذه الصلاحية ضرورية لعرض المقبض الجانبي.

**الخطوات:**

1. افتح **الإعدادات** على جهازك
2. اذهب إلى **التطبيقات** أو **مدير التطبيقات**
3. ابحث عن **NexusClip**
4. اضغط على **صلاحيات خاصة** أو **أذونات إضافية**
5. اختر **العرض فوق التطبيقات الأخرى**
6. فعّل الخيار

**أو من داخل التطبيق:**
- سيُطلب منك تفعيل الصلاحية عند أول تشغيل
- اضغط على "تفعيل" وسيتم توجيهك للإعدادات

### صلاحية خدمة الوصول (مستحسنة)

هذه الصلاحية تتيح مراقبة الحافظة والتحكم بالمؤشر.

**الخطوات:**

1. افتح **الإعدادات**
2. اذهب إلى **إمكانية الوصول** أو **الوصول**
3. ابحث عن **NexusClip** في قائمة الخدمات
4. اضغط عليها وفعّل الخيار
5. اقرأ التحذير واضغط **موافق**

**ملاحظة مهمة:**
- بعض الأجهزة تتطلب إعادة التشغيل بعد تفعيل خدمة الوصول
- إذا لم تعمل الميزة فوراً، أعد تشغيل الجهاز

---

## ✅ التحقق من التثبيت / Verify Installation

### اختبار المقبض الجانبي

1. بعد تفعيل الصلاحيات، يجب أن يظهر مقبض ذهبي على حافة الشاشة اليمنى
2. اسحب المقبض للداخل لفتح الشريط الجانبي
3. اضغط على المقبض لفتح الخزنة الكاملة

### اختبار مراقبة الحافظة

1. انسخ أي نص من أي تطبيق
2. افتح NexusClip
3. يجب أن يظهر النص المنسوخ في تبويب "Live"

### اختبار Virtual D-Pad

1. افتح أي تطبيق محرر نصوص
2. افتح شريط NexusClip الجانبي
3. اضغط على أزرار الأسهم
4. يجب أن يتحرك المؤشر في حقل النص

---

## 🖥️ تثبيت Linux Companion

### المتطلبات

```bash
Python 3.7+
pip (Python package manager)
```

### التثبيت

```bash
# انتقل لمجلد linux_companion
cd linux_companion

# تثبيت الاعتمادات
pip install pyperclip zeroconf

# أو باستخدام pip3
pip3 install pyperclip zeroconf
```

### على أنظمة Linux المختلفة

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install python3 python3-pip xclip
pip3 install pyperclip zeroconf
```

**Fedora/RHEL:**
```bash
sudo dnf install python3 python3-pip xclip
pip3 install pyperclip zeroconf
```

**Arch Linux:**
```bash
sudo pacman -S python python-pip xclip
pip install pyperclip zeroconf
```

### التشغيل

```bash
# تشغيل عادي
python3 nexusclip_daemon.py

# تشغيل في الخلفية
python3 nexusclip_daemon.py &

# تشغيل مع systemd (اختياري)
# انظر قسم "تشغيل كخدمة نظام"
```

### التشغيل كخدمة نظام (Systemd)

1. أنشئ ملف الخدمة:

```bash
sudo nano /etc/systemd/system/nexusclip.service
```

2. أضف المحتوى التالي:

```ini
[Unit]
Description=NexusClip Linux Companion
After=network.target

[Service]
Type=simple
User=YOUR_USERNAME
WorkingDirectory=/path/to/nexusclip/linux_companion
ExecStart=/usr/bin/python3 nexusclip_daemon.py
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
```

3. فعّل وشغّل الخدمة:

```bash
sudo systemctl daemon-reload
sudo systemctl enable nexusclip
sudo systemctl start nexusclip

# التحقق من الحالة
sudo systemctl status nexusclip
```

---

## 🔧 إعدادات جدار الحماية / Firewall Settings

### Linux (UFW)

```bash
# السماح بـ UDP على Port 4040
sudo ufw allow 4040/udp

# التحقق
sudo ufw status
```

### Linux (firewalld)

```bash
# السماح بالمنفذ
sudo firewall-cmd --add-port=4040/udp --permanent
sudo firewall-cmd --reload
```

### Linux (iptables)

```bash
# السماح بالمنفذ
sudo iptables -A INPUT -p udp --dport 4040 -j ACCEPT
```

---

## 🔄 التحديث / Update

### تحديث التطبيق

```bash
# إلغاء التثبيت القديم (اختياري)
adb uninstall com.nexusclip.clip

# تثبيت النسخة الجديدة
adb install nexusclip-release.apk
```

### تحديث Linux Companion

```bash
cd linux_companion
git pull origin main
pip3 install -r requirements.txt --upgrade
```

---

## ❓ الأسئلة الشائعة / FAQ

### س: لماذا يحتاج التطبيق صلاحية العرض فوق التطبيقات؟
**ج:** لعرض المقبض الجانبي الذي يظهر دائماً على حافة الشاشة.

### س: هل صلاحية خدمة الوصول آمنة؟
**ج:** نعم. NexusClip:
- لا يرسل أي بيانات للإنترنت
- لا يخزن بيانات خارج جهازك
- يستخدم الصلاحية فقط لمراقبة الحافظة والتحكم بالمؤشر

### س: لماذا لا يظهر المقبض؟
**ج:** تأكد من:
1. تفعيل صلاحية العرض فوق التطبيقات
2. تشغيل الخدمة (افتح التطبيق مرة واحدة)
3. عدم وجود تطبيق آخر يحجب الشاشة

### س: هل يعمل التطبيق على جميع أجهزة Android؟
**ج:** يعمل على Android 8.0 (API 26) وأحدث.

### س: كيف أوقف الخدمة مؤقتاً؟
**ج:** من إعدادات التطبيق → إيقاف الخدمة، أو من إشعار الخدمة.

---

## 📞 الدعم / Support

إذا واجهت أي مشكلة:
1. راجع ملف [TROUBLESHOOTING.md](TROUBLESHOOTING.md)
2. افتح Issue على GitHub
3. تواصل معنا عبر البريد الإلكتروني
